{
  "name": "enderr-stats-api",
  "version": "1.0.0",
  "description": "Stats receiver API for og-task-bot, served at enderr.win/api/stats/og-task-bot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18"
  }
}
